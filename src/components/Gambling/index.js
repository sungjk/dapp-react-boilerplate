import React from 'react';

const Chat = ({ chats }) => {
    return (
        <div className="container">
            Chats
        </div>
    );
};

Chat.propTypes = {
};
Chat.defaultProps = {
};

export default Chat;
